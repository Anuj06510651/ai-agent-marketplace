{
  "name": "ai-agent-marketplace-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start": "echo \"Please cd into client or server depending on what you want to deploy.\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
